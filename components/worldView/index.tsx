import {
  Container,
  Box
} from "@material-ui/core";

export default function WorldView(): React.ReactElement {
    return (
        <Container fixed>
            <Box>
                <h1>Hello, World!</h1>
            </Box>
        </Container>
    );
}
